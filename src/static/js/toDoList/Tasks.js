
/**
 * Represents a task.
 * @param {string} title 
 * @param {bool} status - 2 choices: True if done, false if not.
 */
class Task {
    constructor(title, status) {
        this.title = title;
        this.status = status;
    }
    
}

export { Task };
